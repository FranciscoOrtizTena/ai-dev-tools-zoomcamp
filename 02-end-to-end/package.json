{
  "name": "snake-ops-dev",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently -k -n api,web \"cd backend && uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000\" \"cd frontend && npm run dev -- --host --port 5173\"",
    "dev:backend": "cd backend && uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "dev:frontend": "cd frontend && npm run dev -- --host --port 5173"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
